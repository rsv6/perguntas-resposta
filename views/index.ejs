<%- include ('partials/header.ejs') %>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <hr>
        <h1>Perguntas</h1>
        <hr>
        <a href="/perguntar" class="btn btn-primary">Perguntar</a>
        <hr>
        <% perguntas.map(p => { %>
            <br>
            <div class="card">
                <div class="card-body">
                    <h3><%= p.titulo %></h3>

                </div>
                <div class="card-footer">
                    <a class="btn btn-primary" href="/pergunta/<%= p.id %>">Responder</a>
                </div>
            </div>
            <!-- <p><%= p.descricao %></p> -->
        <% }) %>
    </div>
</body>
<%- include ('partials/footer.ejs') %>
